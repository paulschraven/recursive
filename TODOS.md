# TODOS — /recursive v2

Generated by `/plan-devex-review` on 2026-04-20. All items below have been explicitly decided in-session.

---

## P0 — blocker, ship before anyone else runs /recursive

### 1. Replace placeholder text at recursive.md:378 and :380
- **What:** `[STATIC_QR_BLOCK_PLACEHOLDER]` and `Apply directly: [FORM_LINK_PLACEHOLDER]` are literal strings in the final card output every user sees.
- **Why:** Blocks the application funnel at the last step. User hits "ship it," gets a card with broken text, closes the tab.
- **Fix:** Generate the QR with `qrencode -t ANSIUTF8 -m 2 "<form URL>"`, paste into recursive.md replacing the placeholder block. Replace both `[FORM_LINK_PLACEHOLDER]` occurrences with the real Tally URL.

---

## P1 — v2, commit before next share cycle

### 2. Reframe hash as vanity suffix; drop "fingerprint / unfakeable" language
- **What:** The Recursive ID's 4-hex suffix is non-deterministic (CLAUDE.md edits change it) and trivially forgeable (public skill = 2-line reverse).
- **Why:** Current README line 7 ("your stack signature is your fingerprint, your fingerprint is your invite") and recursive.md:270 ("This is the 'unfakeable' anchor") make false claims.
- **Fix:** Edit README.md line 7 to: "6 archetypes. 4 dimensions. 1 card. Your card is your ticket." Delete the "unfakeable" comment at recursive.md:270. Keep the hash computation — it still looks cool on the card.

### 3. Tag releases + curl from `/latest/` + add version footer
- **What:** Move install off `raw.githubusercontent.com/.../main/recursive.md`. Cut `v1.0.0` etc. Install command fetches the latest release asset.
- **Why:** Users who curled last week silently stay broken when a fix ships. A skill whose output IS the invite ticket must keep users current.
- **Fix:** Cut GitHub releases with `recursive.md` as a release asset. Install command becomes something like:
  ```
  curl -o ~/.claude/commands/recursive.md \
    "$(curl -s https://api.github.com/repos/paulschraven/recursive/releases/latest | jq -r '.assets[0].browser_download_url')"
  ```
  Add footer to card: `v1.2 · /recursive`.

### 4. Add post-install verify line + hardened install instructions
- **What:** README install section gives no confirmation the curl worked.
- **Fix:** Add one line under the install command: `Verify: open Claude Code and run /recursive. You'll see the R E C U R S I V E banner.`

### 5. Add one-line attribution + LinkedIn
- **What:** No maintainer info on README. YC-founder persona squints before curling.
- **Fix:** One line under the tagline: `By Paul Schraven · [LinkedIn](https://linkedin.com/in/paulschraven)`. Keeps minimalism, adds trust.

### 6. Sample card screenshot in README above install block
- **What:** README is all text. No pre-install proof of what output looks like.
- **Fix:** Run the skill, screenshot the card for a representative archetype, paste as image at top of README. Same pattern as `onefetch` / `neofetch` repos.

### 7. Fix MCP scan false-positive + drop `$(pwd)` paths
- **What:** recursive.md:59-62 fallback greps `"command"` across settings.json files — counts HOOK definitions as MCP servers. Also references `$(pwd)/.mcp.json` and `$(pwd)/.claude/settings.json` which are meaningless (Claude Code cwd at invocation time is arbitrary).
- **Fix:** Replace grep fallback with jq-based parsing scoped to `mcpServers` block. Drop project-level paths; only scan `~/.claude.json` and `~/.claude/settings.json`. Graceful-degrade: if jq is absent and `claude mcp list` fails, show `?` and add reaction line "MCP count unclear — tell us in Q4."

### 8. Add "here's what I heard" confirmation turn after Q4
- **What:** Step 6 distills Q4 silently into bullets that go straight onto the draft card. Redact loop catches "remove company name" but not "you misread what I said."
- **Fix:** New step after Step 6: output `Here's what I caught: [bullets]. Nail it, or tell me what I got wrong.` Accept affirmatives (`nailed it`, `close enough`, `ship`) or edit instructions. Loop once.

### 9. Flip QR / hyperlink order in join footer; keep QR as aesthetic
- **What:** QR is useless for same-device applicants (user is on laptop, needs a second phone to scan).
- **Fix:** Print `Apply: https://...` as primary CTA first (clickable in Claude Code terminal). QR below, labeled "or scan from another device."

### 10. Rewrite Apprentice + Pragmatist verdict copy for share-worthy tone
- **What:** recursive.md:253 "Early. Hungry. You ran /recursive, which means the instinct is there..." may read as rejection rather than an affectionate nudge.
- **Why:** Every archetype verdict should be something the user would forward to a peer, not quietly close.
- **Fix:** Rewrite both lines so they land as warm. Example direction — Apprentice: "Early, but you showed up on the right night. Build one thing this week that builds the next thing." Pragmatist: "Solid across the board. The kind of builder everyone actually wants in the group chat."

### 11. Tighten Step 0 acknowledgement parsing
- **What:** recursive.md:45 says "read the user's reply intelligently — don't require a magic word" which is LLM-fragile. Someone who types "yes, but first a question" may be wrongly treated as proceeding.
- **Fix:** Replace with explicit rule: "If the reply is clearly affirmative (yes / ok / ready / go / sure / let's), proceed. If it's a question, hesitation, or pushback, answer briefly and re-ask 'Ready?'"

### 12. Reconcile brand-block subline drift at recursive.md:11
- **What:** The recent commit message "remove tagline subline" says it was removed, but the `Subline:` definition still exists at line 11.
- **Fix:** Decide which is truth. Either keep the subline and update the commit narrative, or remove the subline definition and any downstream references.

---

## P2 — v2 nice-to-have, polish

### 13. Add LICENSE file
- **What:** No LICENSE. Hurts people who want to fork, reference, or redistribute.
- **Fix:** Add MIT license file.

### 14. Add 3-line FAQ block to README
- **Fix:** Under "Community" section:
  - Q: Card feels wrong? — A: Re-run, or DM @paulschraven.
  - Q: Didn't hear back? — A: Applications are reviewed weekly.
  - Q: Found a bug? — A: Open an issue or ping me on LinkedIn.

### 15. Extend re-ask logic from Q1 to Q2/Q3/Q4
- **What:** recursive.md:137 re-asks once on invalid Q1 input. Q2/Q3/Q4 don't.
- **Fix:** Apply the same pattern to the other questions.

### 16. Shorten Q1 option 5
- **What:** recursive.md:130-132 option 5 is 3 physical lines long, visually intimidating next to 1-line options.
- **Fix:** Compress to 1-2 lines without losing the "infrastructure that writes infrastructure" bite.

### 17. Add CHANGELOG.md
- **Fix:** Track each tagged release. Ship alongside TODO #3.

---

## P3 — post-v2, observe before committing

### 18. File-count-as-identity category-error tension (DEFERRED)
- **What:** Outside voice flagged that a senior CTO on a minimal Claude Code setup scores as Apprentice; a junior with bloated dotfiles scores as Architect.
- **Why deferred:** You chose to keep the quiz + card as designed (the ritual is the product). Revisit after observing real v1 applications: if "obviously wrong archetype" assignments are a recurring complaint, reopen.

### 19. Anti-resubmission binding on Tally form (DEFERRED)
- **What:** Nothing ties a submitted screenshot to the submitter — someone can apply with a peer's card.
- **Why deferred:** You chose not to ship this in v2. Admin-side review catches obvious duplicates for now.

### 20. Drop WhatsApp# from apply form (DEFERRED)
- **What:** Asking for WhatsApp# with country code at apply time is a higher-friction PII ask than LinkedIn.
- **Why deferred:** You chose to keep it. Revisit if form-abandon rate is high.

### 21. Scan-cross-check as soft archetype downgrade (DEFERRED)
- **What:** Implement recursive.md:113's silent cross-check as a real one-tier downgrade when Q3≥4 but scan shows low commands+skills+mcps.
- **Why deferred:** You chose to keep the quiz + card as designed. The current soft cross-check stays silent.

### 22. DX measurement baseline
- **What:** Pass 8 scored 5/10. No in-skill instrumentation (violates privacy brand). Only external signal is GitHub clone count vs Tally submission count.
- **Fix:** Manually track those two numbers weekly. Drop-off = clones − submissions. Over time, this reveals where the funnel breaks.

### 23. Pre-install 15s asciinema / GIF
- **What:** Would push Pass 1 from 9/10 to 10/10.
- **Why deferred:** Static screenshot (TODO #6) captures 80% of the value; GIF is polish.

### 24. Hash collision at community scale
- **What:** 4 hex chars = 65k space. Fine under ~200 members, non-trivial collisions past ~1000.
- **Fix:** Not needed if hash is reframed as vanity suffix (TODO #2). Revisit if you ever need it to be unique at scale.
